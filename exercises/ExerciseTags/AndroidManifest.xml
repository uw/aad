<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aad.app.exercise.tags"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.NFC" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <!-- TODO 2.1 Add an NDEF_DISCOVERED IntentFilter for MainActivity -->
        <activity
            android:name="aad.app.exercise.tags.MainActivity"
            android:label="@string/main" >
            
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="main"
                    android:scheme="xs" />
            </intent-filter>
            
        </activity>
        
        <!-- TODO 2.2 Add an NDEF_DISCOVERED IntentFilter for TestActivity -->
        <activity
            android:name="aad.app.exercise.tags.TestActivity"
            android:label="@string/test" >

            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="test"
                    android:scheme="xs" />
            </intent-filter>
            
        </activity>
        
    </application>

</manifest>