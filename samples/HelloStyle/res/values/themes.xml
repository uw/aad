<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="Theme.Custom.BaseTheme" parent="android:Theme">
        <!-- Set older backward-compatible customizations here.  Use /res/values-v##/styles.xml for newer style customizations. -->
    </style>

    <!-- Custom Theme -->
    <style name="CustomTheme" parent="Theme.Custom.BaseTheme">

        <!-- Set API-level agnostic customizations here. --> 
               
        <item name="android:buttonStyle">@style/Theme.Custom.Button</item>
        <item name="android:editTextStyle">@style/Theme.Custom.EditText</item>
        <item name="android:textViewStyle">@style/Theme.Custom.TextView</item>
        <item name="android:progressBarStyleHorizontal">@style/Theme.Custom.ProgressBar</item>
        <item name="android:windowBackground">@drawable/background_gradient</item>
    </style>

</resources>